#include <stdio.h>

/**
 * main - entry point
 * return: always 0 (success/correct)
 */

int main(void)

{
	char alphabet;

	for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
	{
		if (alphabet == 'q')
			continue;

		else if (alpahbet == 'e')
			continuw;

	putchar(alphabet);
}

putchar('\n');

return (0);

}
	
